# Tools: Research Assistant Agent

| Tool | Purpose |
|------|---------|
| `web_search` | Query a search provider; return ranked snippets |
| `retrieve_document` | RAG retrieval from configured index |
| `store_memory` | Persist session-scoped research notes |
| `cite_source` | Build validated citation records for the final answer |

See individual `*.md` files for JSON Schema and return shapes.
